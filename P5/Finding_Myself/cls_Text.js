class cls_Text {

  /**
   * Constructs a new TEXT object
   * @param {String} text string to display 
  **/
  //@param {Number} x X location of the text on the canvas
  //* @param {Number} y Y location of the text on the canvas
  constructor(text) {
    this.text = new String(text);
    //this.x = x;
    //his.y = y;
    //console.log("Constructor " + text);
  }
}